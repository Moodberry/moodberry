name: "Moodberry"
version: 1.0.0
maintainer: "Klydra Project"
description: "Moodberry is the init system and service manager for Klydra. It replaces systemd as PID 1."

dependencies:
  - moodberryd

source:
  raw: "https://raw.githubusercontent.com/Moodberry/moodberry/main"
  package: "https://github.com/Moodberry/moodberry/releases/download/v1.0.0/moodberryd-1.0.0.choco.pkg"

install:
  commands: |
    sudo mkdir -p /sbin
    sudo ln -sf /opt/bitpuppy/Chocolaterie/moodberryd/bin/moodberryd /sbin/moodberryd
    sudo ln -sf /sbin/moodberryd /sbin/init
